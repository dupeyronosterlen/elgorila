node_modules/
.DS_Store
*.log

# Secretos y credenciales (nunca subir)
.env
.env.*
.dev.vars
server/.env
server/data/
usuarios.json
CREDENCIALES*
PROMPT-CLAUDE*.md

# Documentos internos (estrategia, finanzas, operación, credenciales).
# El repo es la raíz del sitio: TODO lo commiteado queda PÚBLICO en
# https://elgorilateatro.com.mx/<ruta>. Nada interno va aquí dentro.
docs-internos/
.wrangler/

# Previews / artefactos locales
preview/
